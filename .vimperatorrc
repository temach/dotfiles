" notes:
" see jumplist with jl "
"

" speed up "
nnoremap \ :

" allow usual copy/paste"
nnoremap <C-c> <C-v><C-c>
nnoremap <C-v> <C-v><C-v>

" follow hints in new tab by default "
nnoremap F f
nnoremap f F

" you can use 'gt X' to change to tab number X "
set tabnumbers

" press <F4> to toggle search highlight"
nnoremap <F4> :noh<CR><CR>

" use <C-i> to edit text field in vim "
set "editor=urxvt -e vim"

" make normal scrolling faster "
nnoremap j 7j
nnoremap k 7k

"<C-e> and <C-y> scroll the viewport a single line. Make them faster.
nnoremap <C-e> 10j
nnoremap <C-y> 10k



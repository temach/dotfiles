" notes:
" see jumplist with jl "
"

" mapleader "
let mapleader = ","

" speed up "
nnoremap \ :

"" allow usual copy/paste"
"nnoremap <C-c> <C-v><C-c>
"nnoremap <C-v> <C-v><C-v>

" follow hints in new tab by default "
nnoremap F f
nnoremap f F

" you can use 'gt X' to change to tab number X "
" set tabnumbers

" press <F4> to toggle search highlight"
nnoremap <Leader>, :noh<CR><CR>

" use <C-i> to edit text field in vim "
set "editor=urxvt -e vim"

" make normal scrolling faster "
nnoremap j 5j
nnoremap k 5k
nnoremap l 2l
nnoremap h 2h

"<C-e> and <C-y> scroll the viewport a single line. Make them faster.
nnoremap <C-e> 10j
nnoremap <C-y> 10k

" hide gui except for tabs"
set gui=none,tabs

" set default search to duckduckgo "
set defsearch=duckduckgo

" show big gui"
nnoremap <Leader>b ":set gui=all<CR>"

" show small gui "
nnoremap <Leader>s ":set gui=none,tabs<CR>"

